/*#include"Insere_conexoes.hpp"
#include"estruturas.hpp"
#include<stdio.h>
#include<stdlib.h>
#include"le_dir.hpp"

Rede* obtem_dados(FILE *arquivo,char* dado,int p_x,int p_y,int x_f,int y_f,int rec, int cust){
    Rede* r = (Rede*)malloc(sizeof(Rede));
    Rede->comeco = NULL;
    Rede->inic=NULL;
    if(dado[0]!='I'){
        Rede->comeco = insere_elemento(Rede->comeco,arquivo,dado,p_x,p_y,rec,cust);
    }else{
        Rede->inic = insere_con(Rede->inic,arquivo,dado,p_x,p_y,x_f,y_f,rec,cust);
    }
    return r;
}
*/
